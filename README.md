# 桝Happy

集水桝の一括生成ツール群です。

## 概要
- `00_Masu_Tougou.py` を起点に、条件入力から DXF/数量計算書までをまとめて生成できます。
- 生成結果は `output_Masu_*` 配下に保存されます。

## 使い方
1. Python 3.10 以上を用意します。
2. `python 00_Masu_Tougou.py` を実行します。
3. 対話形式で条件を入力し、出力フォルダを確認します。

## 依存関係
- 標準ライブラリのみを使用しています。

## 注意
- 実行には `tools/` 配下の補助スクリプトが必要です。
